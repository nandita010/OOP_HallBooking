package com.example.oop_hallbooking.Service;

import java.util.ArrayList;
import java.util.List;
/*

@Service

public class ReservationService {

    final private ArrayList<Reservation> reservations = new ArrayList<>();
    private long nextID = 1;

    public Reservation addReservation(Reservation reservation) {

        reservation.setReservationID(nextID++);
        Reservation.add(reservation);
        return reservation;

    }


    public List<Reservation> getAllReservations(long reservationID) {
        return reservations;
    }

    public Reservation getReservationByID(long reservationID) {
        Reservation tempReservation = null;

        for (Reservation rs : reservations) {
            if (rs.getReservationID() == reservationID) {
                tempReservation = rs;
            }

        }
        return tempReservation;
    }

*/


